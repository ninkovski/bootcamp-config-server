server:
  port: 9999
  
spring:
  application:
    name: 'config-server'  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ninkovski/bootcamp-config-repo.git
          search-paths: '{application}'